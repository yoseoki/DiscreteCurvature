[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "ml-research"
version = "0.1.0"
requires-python = ">=3.11"

[tool.hatch.build.targets.wheel]
packages = ["src/ml_research"]
